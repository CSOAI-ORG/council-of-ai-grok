#!/usr/bin/env node
// Passive SessionStart hint. Never blocks. Never invents scores.
process.stderr.write(
  "[council-of-ai] Measurement plugin loaded. /council · /gspc · /verify · /sign — we measure, we do not certify.\n"
);
process.exit(0);
